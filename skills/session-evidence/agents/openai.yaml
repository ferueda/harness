interface:
  display_name: "Session Evidence"
  short_description: "Extract snippets and artifacts from local agent sessions"
  default_prompt: "Use $session-evidence to run Cursor or Codex extract-only transcript searches with repeatable turn queries, collect matching snippets and artifacts, and report session ids with turn indexes."
