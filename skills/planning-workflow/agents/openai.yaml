interface:
  display_name: "Planning Workflow"
  short_description: "Route work to planning or direct implementation"
  default_prompt: "Use $planning-workflow to choose the next useful action and implement directly when no planning artifact is needed."
policy:
  allow_implicit_invocation: false
