interface:
  display_name: "Planning Workflow"
  short_description: "Route intent through planning, review, and implementation"
  default_prompt: "Use $planning-workflow to route this work from intent through a reviewed, minimum-sufficient plan and implementation."
