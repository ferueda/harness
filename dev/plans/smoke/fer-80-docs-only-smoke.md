# FER-80 documentation-only CI smoke

Disposable pull-request fixture used to verify that a Markdown-only change under
`dev/plans/**` runs plan checks and skips the full CI gate.

This branch is never merged.
